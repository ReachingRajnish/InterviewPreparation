using System;
using System.Collections.Generic;
using System.Text;

namespace InterviewPreparation.Extension_Method
{
    #region Extension Method

    /*
     
    public static class MerchantAccountExtensions
    {
        public static MerchantAccountViewModel ToMerchantAccountViewModel(this MerchantAccount merchantAccount)
        {
            return new MerchantAccountViewModel
            {
                Id = merchantAccount.Id,
                SourceId = merchantAccount.SourceId,
                CountryCode = merchantAccount.CountryCode,
                CurrencyCode = merchantAccount.CurrencyCode,
                LocaleCode = merchantAccount.LocaleCode,
            }
        }
    }

    */

    #endregion 
}
